module.exports = async (sock, chatId) => {    const check = (condition, message) =>         condition ? `✅ ${message}` : `❌ ${message}`;        const report = `🩺 *BOT HEALTH CHECK*${check(sock.connection, 'Connection active')}${check(fs.existsSync('./session'), 'Session exists')}${check(process.uptime() > 60, 'Stable uptime')}${check(process.memoryUsage().rss < 300000000, 'Memory normal')}💡 Tip: Use .antibug for detailed diagnostics    `;        await sock.sendMessage(chatId, { text: report });};